
<div class="row">
    <div class="col">
      <div class="menu-container">
        <ul>
          {% for item in items %}
            <li class="{{item.class}}">
              <a  href="{{item.href}}">{{item.title}}</a>
            </li>
          {% endfor %}
        </ul>
      </div>
    </div>
  </div>
