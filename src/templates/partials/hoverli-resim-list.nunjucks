<div id="images"
  style="display: flex;
    -ms-flex-wrap: wrap;
    flex-wrap: wrap;
    margin-right: -15px;
    margin-left: -15px;">
{% for item in items %}
  <div class="{{item.class}}">
    <div class="galeri-foto-container" onclick="previewImg('{{item.id}}')">
      <img id="{{item.id}}" src="{{item.src}}" class="img-fluid galeri-fotolar"   >
      <a  class="foto-buyuk"  style="pointer-events:none;">
        <img hidden="true" src="img/foto-hover.png">
        <br><br>
                  BÜYÜK HALİ İÇİN TIKLAYIN
                </a>
    </div>  
  </div>
{% endfor %}
</div>

 <script src="viewer/viewer.js"></script>
  <script>
    function previewImg(imageId) {
      /*
      console.log(imageId)
      const viewer = new Viewer(document.getElementById(imageId), {
        viewed() {
          viewer.zoomTo(1);
        }
      });
      */
    }
    const gallery = new Viewer(document.getElementById('images'), { filter(image) {
      console.log(image.hidden)
      if (!image.hidden)
        return image
    } });

  </script>